{
  "name": "express-k8s-demo",
  "version": "1.0.0",
  "description": "Simple Express app demonstrating Docker build/push, Kubernetes deployment, and a Jenkins CI/CD pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"No automated tests defined yet\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
